package otherpractice;


/**
 * A doubly linked list weaved into a binary search tree. Assumes monotonically increasing keys (like real-time
 * timestamps) are inserted.
 */
public class OrderedSearchTree {
}
